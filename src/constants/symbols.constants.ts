// Used to store the base path metadata for a class.
export const BASE_PATH_SYMBOL = Symbol('base-path');

// Used to store the route path metadata for a class.
export const ROUTER_PATHS_INFO_SYMBOL = Symbol('router-path-info');

// Used to mark a class as a resource.
export const Z_RESOURCE_SYMBOL = Symbol('z-resource');

// Used to store the dependencies metadata for a class.
export const DEPENDENCIES_SYMBOL = Symbol('dependencies');

// Used to mark a class as a service.
export const INJECTABLE_SYMBOL = Symbol('injectable');

// Used to mark a class as a service.
export const Z_SERVICE_SYMBOL = Symbol('z-service');

// Used to mark a class as a repository.
export const Z_REPO_SYMBOL = Symbol('z-repo');
